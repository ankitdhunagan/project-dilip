//create ten folder named foder 1, folder 2 and so on
//Each of these folder should contain a file with text "I am in folder n", n being the number of folder they are in.

class Classwork
{

    public void Createfolder()
    {
        // string folderPath = @"I:\New folder\.NET-ankit";
        // string rootFolderPath = folderPath + "\\RootFolder";
        // Directory.CreateDirectory(rootFolderPath);
        // for (int i = 1; i < 10; i++)
        // {
        //     string newfolder = rootFolderPath + "\\Folder" + i;
        //     Directory.CreateDirectory(newfolder);
        // }


        // for (byte counter = 0; counter <= 10; counter++)
        // {
        //     Directory.CreateDirectory(@"I:\New folder\.NET-ankit\folder{counter}");
        // }
        
        
        // for (byte counter = 0; counter <= 10; counter++)
        // {
        //     string folderName = "Folder" + counter;
        //     string folderPath = @$"D:\App"
           
        // }


    }

    
      

    




}